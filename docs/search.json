[
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "John Fee",
    "section": "",
    "text": "Welcome to my website! It’s under construction…"
  },
  {
    "objectID": "about.html",
    "href": "about.html",
    "title": "About",
    "section": "",
    "text": "About this site\n\n1 + 1\n\n[1] 2"
  },
  {
    "objectID": "projects.html",
    "href": "projects.html",
    "title": "My Projects",
    "section": "",
    "text": "Colored ggplot2 legend text with Marquee\n\n\n\n\n\n\nJohn Fee\n\n\nJun 16, 2024\n\n\n\n\n\n\n\n\n\n\n\n\nSimulating collapsibility\n\n\n\n\n\n\nJohn Fee\n\n\nFeb 27, 2024\n\n\n\n\n\n\n\n\nNo matching items"
  },
  {
    "objectID": "projects/Colored ggplot2 text with Marquee/legends_with_marquee.html",
    "href": "projects/Colored ggplot2 text with Marquee/legends_with_marquee.html",
    "title": "Colored ggplot2 legend text with Marquee",
    "section": "",
    "text": "I essentially follow this walkthrough by Nicole Rennie. Note that getting the element_marquee to render properly required up to date versions of R and RStudio as of 6/16/2024.\n\nlibrary(dplyr)\nlibrary(glue)\nlibrary(ggplot2)\nlibrary(marquee)\ntheme_set(ggpubr::theme_pubr())\n\nset.seed(1)\n\n\nbar_df &lt;- iris |&gt;\n  # Let's collect 25 specimens while pretending that the iris dataset is our population\n  slice_sample(n = 25) |&gt;\n  count(Species) |&gt;\n  mutate(\n    Species = Species |&gt;\n      as.character() |&gt;\n      snakecase::to_any_case(case = \"sentence\"),\n    percentage = scales::label_percent()(n/sum(n)),\n    my_label = glue(\"{n} ({percentage})\")\n  )\n\nmy_palette &lt;- RColorBrewer::brewer.pal(length(bar_df$Species),\"Set1\")\nnames(my_palette) &lt;- unique(bar_df$Species)\n\nmake_group_string &lt;- function(my_palette){\n  # Need to format my groups into a single string that uses marquee\n  # syntax to color the appropriate bits\n  formatted_strings &lt;- glue(\n    \"{.[my_palette] [names(my_palette)]}\",\n    .open = \"[\",\n    .close = \"]\"\n  )\n\n  all_but_last &lt;- formatted_strings[1:(length(formatted_strings) - 1)]\n  last_element &lt;- formatted_strings[length(formatted_strings)]\n\n  paste(all_but_last,collapse = \", \") |&gt;\n    paste0(\", and \",last_element)\n}\n\nbar_df |&gt;\n  mutate(\n    y_label = n + max(n)/20\n  ) |&gt;\n  ggplot(aes(x = Species,y = n,fill = Species,label = my_label)) +\n  geom_col() +\n  geom_text(aes(y = y_label)) +\n  scale_fill_manual(name = \"Species\",values = my_palette) +\n  scale_y_continuous(\n    \"# collected\",\n    breaks = seq(0,10,2),\n    limits = c(0,10)\n  ) +\n  labs(\n    title = \"What is the distribution of iris species in our sample?\",\n    subtitle = glue::glue(\"The species collected include {make_group_string(my_palette)}\")\n  ) +\n  theme(\n    plot.subtitle = element_marquee(),\n    legend.position = \"None\"\n  )"
  },
  {
    "objectID": "projects/collapsibility/Simulating Collapsibility.html",
    "href": "projects/collapsibility/Simulating Collapsibility.html",
    "title": "Simulating collapsibility",
    "section": "",
    "text": "# I/O\nlibrary(here)\n\n# Data manipulation\nlibrary(dplyr)\nlibrary(purrr)\nlibrary(tidyr)\n\n# Graphics\nlibrary(ggplot2)\nlibrary(ggdag)\nlibrary(dagitty)\nlibrary(latex2exp)\ntheme_set(cowplot::theme_cowplot())\n\n# Misc\nsource(here(\"projects\",\"collapsibility\",\"sim_functions.R\"))\nset.seed(1)\nI’m going to walk through a few different scenarios with different data generating processes, and via simulation examine how well I can recover the true value of the parameter of interest."
  },
  {
    "objectID": "projects/collapsibility/Simulating Collapsibility.html#scenario-1---no-confounders",
    "href": "projects/collapsibility/Simulating Collapsibility.html#scenario-1---no-confounders",
    "title": "Simulating collapsibility",
    "section": "Scenario 1 - No confounders",
    "text": "Scenario 1 - No confounders\nFor our DGP, assume that I observe the values of a continuous response variable \\(Y\\), a binary treatment variable \\(X\\), and measured covariates \\(W\\) and \\(Z\\). \\(X\\), \\(Y\\), and \\(Z\\) are mutually independent, but \\(Y\\) is dependent upon all of them. This dependency structure can be represented graphically in the form of a DAG.\n\ndagify(\n  Y ~ X,\n  Y ~ W,\n  Y ~ Z\n) %&gt;%\n  ggdag() +\n  theme_void()\n\n\n\n\n\n\n\n\nTo simulate data compatible with this structure, I draw the predictors from a multivariate normal distribution with a diagonal covariance matrix (i.e. they are independent draws from separate normal distributions). \\(X\\) is thresholded to convert it into a binary variable, and \\(Y\\) is generated by the following equations\n\\[\n\\begin{aligned}\n  Y &= X + 3W + 1.5Z + \\varepsilon\\\\\n  \\varepsilon &\\sim \\mathcal{N}(0,9)\n\\end{aligned}\n\\]\nThis process is repeated until 1000 datasets of size \\(n = 500\\) are generated. I then fit the same linear model to each simulated dataset, and plot the distribution of the results and provide a table of summary stats.\n\ncov_matrix_diagonal &lt;- matrix(\n  c(\n    4,0,0,\n    0,4,0,\n    0,0,4\n  ),\n  nrow = 3,\n  ncol = 3,\n  byrow = TRUE\n)\n\nresults &lt;- simulate_multiple_datasets(\n  n = 500,\n  sigma = cov_matrix_diagonal,\n  n_simulations = 1000,\n  simulate_function = simulate_linear_model,\n  y_formula = 3*W + X + 1.5*Z,\n  x_threshold = qnorm(0.5), # Chosen so X is a 50/50 split in both treatment groups\n  epsilon_sd = 9\n) %&gt;%\n  mutate(\n      fitted_model = map(data,~ lm(Y ~ X + Z + W,data = .x))\n    )\n\n\ntrue_coefs &lt;- data.frame(\n  parameter = c(\"(Intercept)\",\"X\",\"Z\",\"W\"),\n  true_value = c(0,1,1.5,3)\n  )\n\ncoef_df &lt;- true_coefs %&gt;%\n  get_coef_df(results,.)\n\nplot_coefficient_distribution(\n  coef_df,\n  title = \"Distribution of linear regression parameter estimates \\nfit on data simulated from the same linear model\"\n  )\n\n\n\n\n\n\n\n\n\n\n\nSummary table for parameters of \\(n = 1000\\) linear models fit on simulated data\n\n\nVariable\nParameter estimate\nTrue value\nBias\n\n\n\n\n(Intercept)\n0.0023046\n0.0\n0.0023046\n\n\nW\n2.9840192\n3.0\n-0.0159808\n\n\nX\n1.0034573\n1.0\n0.0034573\n\n\nZ\n1.4959494\n1.5\n-0.0040506\n\n\n\n\n\nAs expected, the linear model provides unbiased estimates of the effect associated with our treatment variable \\(X\\). Varying the dataset size or the amount of noise (variance of \\(\\varepsilon\\)) would change the shape of the simulated parameter distributions, but they would remain unbiased. What if we have a more complicated dependency structure?"
  },
  {
    "objectID": "projects/collapsibility/Simulating Collapsibility.html#scenario-2---confounders-present",
    "href": "projects/collapsibility/Simulating Collapsibility.html#scenario-2---confounders-present",
    "title": "Simulating collapsibility",
    "section": "Scenario 2 - Confounders present",
    "text": "Scenario 2 - Confounders present\nLet’s take the same setup as scenario 1, but instead \\(X\\) and \\(Y\\) both depend on \\(Z\\).\n\ndagify(\n  Y ~ X,\n  Y ~ W,\n  Y ~ Z,\n  X ~ Z\n) %&gt;%\n  ggdag() +\n  theme_void()\n\n\n\n\n\n\n\n\nIt is sufficient to condition on \\(X\\) and \\(Z\\) to recover the correct coefficient for \\(X\\). Let’s simulate the data and model fitting and check that this this true.\n\ncov_matrix_confounding &lt;- matrix(\n  c(\n    4,0,0,\n    0,4,3,\n    0,3,4\n  ),\n  nrow = 3,\n  ncol = 3,\n  byrow = TRUE\n)\n\nresults &lt;- simulate_multiple_datasets(\n  n = 500,\n  sigma = cov_matrix_confounding,\n  n_simulations = 1000,\n  simulate_function = simulate_linear_model,\n  y_formula = 3*W + X + 1.5*Z,\n  x_threshold = qnorm(0.5), # Chosen so X is a 50/50 split in both treatment groups\n  epsilon_sd = 9\n) %&gt;%\n  mutate(\n      fitted_model = map(data,~ lm(Y ~ X + Z + W,data = .x))\n    )\n\n\ncoef_df &lt;- true_coefs %&gt;%\n  get_coef_df(results,.)\n\nplot_coefficient_distribution(\n  coef_df,\n  title = \"Distribution of linear regression parameter estimates \\nfit on data simulated from the same linear model\"\n  )\n\n\n\n\n\n\n\n\n\n\n\nSummary table for parameters of \\(n = 1000\\) linear models fit on simulated data with confounder\n\n\nVariable\nParameter estimate\nTrue value\nBias\n\n\n\n\n(Intercept)\n0.0333366\n0.0\n0.0333366\n\n\nW\n2.9993847\n3.0\n-0.0006153\n\n\nX\n0.9296940\n1.0\n-0.0703060\n\n\nZ\n1.5169205\n1.5\n0.0169205\n\n\n\n\n\nThe above simulation matches with our supposition that conditioning on \\(Z\\) will provide unbiased estimates of the coefficient for \\(X\\). Note that the variance of the estimate for \\(X\\) is larger than in scenario 1, when \\(X\\) and \\(Z\\) were uncorrelated (this is expected). Now what happens if we remove \\(W\\), a prognostic variable, from the models I fit, but do not \\(W\\) from the DGP?"
  },
  {
    "objectID": "projects/collapsibility/Simulating Collapsibility.html#scenario-3---confounders-present-prognostic-variable-omitted",
    "href": "projects/collapsibility/Simulating Collapsibility.html#scenario-3---confounders-present-prognostic-variable-omitted",
    "title": "Simulating collapsibility",
    "section": "Scenario 3 - Confounders present, prognostic variable omitted",
    "text": "Scenario 3 - Confounders present, prognostic variable omitted\nI now repeat scenario 3, but remove \\(W\\) from the models I fit to the data.\n\nresults &lt;- simulate_multiple_datasets(\n  n = 500,\n  sigma = cov_matrix_confounding,\n  n_simulations = 1000,\n  simulate_function = simulate_linear_model,\n  y_formula = 3*W + X + 1.5*Z,\n  x_threshold = qnorm(0.5), # Chosen so X is a 50/50 split in both treatment groups\n  epsilon_sd = 9\n) %&gt;%\n  mutate(\n      fitted_model = map(data,~ lm(Y ~ X + Z,data = .x))\n    )\n\n\ncoef_df &lt;- coef_df &lt;- true_coefs %&gt;%\n  get_coef_df(results,.)\n\nplot_coefficient_distribution(\n  coef_df,\n  title = \"Distribution of linear regression parameter estimates \\nfit on data simulated from a model missing a prognostic variable\"\n  ) +\n  lims(x = c(-2.5,5))\n\n\n\n\n\n\n\n\n\n\n\nSummary table for parameters of \\(n = 1000\\) linear models fit on simulated data with confounder present and prognostic variable omitted\n\n\nVariable\nParameter estimate\nTrue value\nBias\n\n\n\n\n(Intercept)\n-0.0118844\n0.0\n-0.0118844\n\n\nX\n1.0254737\n1.0\n0.0254737\n\n\nZ\n1.4841530\n1.5\n-0.0158470\n\n\n\n\n\nCoefficient for \\(X\\) remains unbiased, even though prognostic variable \\(W\\) is removed. This is a property exclusive to regression with identity and log link functions, as we shall see in the following sections."
  },
  {
    "objectID": "projects/collapsibility/Simulating Collapsibility.html#scenario-1---no-confounders-1",
    "href": "projects/collapsibility/Simulating Collapsibility.html#scenario-1---no-confounders-1",
    "title": "Simulating collapsibility",
    "section": "Scenario 1 - no confounders",
    "text": "Scenario 1 - no confounders\nFor this section, I’m going to modify our DGP to be compatible with a different type of model. Assume that I observe the values of a discrete response variable \\(Y\\), a binary treatment variable \\(X\\), and measured covariates \\(W\\) and \\(Z\\). \\(X\\), \\(Y\\), and \\(Z\\) are mutually independent, but \\(Y\\) is dependent upon all of them.\nThe DAG is the same as in scenario 1 for linear regression (since it’s just a graph of dependencies between variables).\n\ndagify(\n  Y ~ X,\n  Y ~ W,\n  Y ~ Z\n) %&gt;%\n  ggdag() +\n  theme_void()\n\n\n\n\n\n\n\n\nTo simulate data compatible with this structure and the logistic regression model, I draw the predictors from a multivariate normal distribution with a diagonal covariance matrix (i.e. they are independent draws from separate normal distributions). \\(X\\) is thresholded to convert it into a binary variable, and \\(Y\\) is generated by the equations below.\n\\[\n\\begin{aligned}\n  A &= X + 3W + 1.5Z\\\\\n  Y_{prob} &= \\frac{1}{1 + \\exp{A}}\\\\\n  Y &= \\text{Bernoulli}(Y_{prob})\n\\end{aligned}\n\\]\n\nresults &lt;- simulate_multiple_datasets(\n  n = 500,\n  sigma = cov_matrix_diagonal,\n  n_simulations = 1000,\n  simulate_function = simulate_logistic_reg_model,\n  y_formula = 3*W + X + 1.5*Z,\n  x_threshold = qnorm(0.5) # Chosen so X is a 50/50 split in both treatment groups\n) %&gt;%\n  mutate(\n    fitted_model = map(data,~ glm(\n      outcome ~ X + Z + W,\n      family = binomial(link = \"logit\"),\n      data = .x))\n    )\n\n\ncoef_df &lt;- coef_df &lt;- true_coefs %&gt;%\n  get_coef_df(results,.)\n\nplot_coefficient_distribution(\n  coef_df,\n  title = \"Distribution of logistic regression parameter estimates \\nfit on data simulated from the same model\"\n  )\n\n\n\n\n\n\n\n\nParameter estimates for logistic regression are biased (upwards) in finite samples, so we expect some positive bias. This isn’t too bad (we could do better with e.g. Firth penalized logistic regression), but we’re mainly interested in differences between the scenarios."
  },
  {
    "objectID": "projects/collapsibility/Simulating Collapsibility.html#scenario-2---confounders-present-1",
    "href": "projects/collapsibility/Simulating Collapsibility.html#scenario-2---confounders-present-1",
    "title": "Simulating collapsibility",
    "section": "Scenario 2 - Confounders present",
    "text": "Scenario 2 - Confounders present\nLet’s take the same setup as scenario 1, but instead \\(X\\) and \\(Y\\) both depend on \\(Z\\).\n\ndagify(\n  Y ~ X,\n  Y ~ W,\n  Y ~ Z,\n  X ~ Z\n) %&gt;%\n  ggdag() +\n  theme_void()\n\n\n\n\n\n\n\n\n\nresults &lt;- simulate_multiple_datasets(\n  n = 500,\n  sigma = cov_matrix_confounding,\n  n_simulations = 1000,\n  simulate_function = simulate_logistic_reg_model,\n  y_formula = 3*W + X + 1.5*Z,\n  x_threshold = qnorm(0.5) # Chosen so X is a 50/50 split in both treatment groups\n) %&gt;%\n  mutate(\n    fitted_model = map(data,~ glm(\n      outcome ~ X + Z + W,\n      family = binomial(link = \"logit\"),\n      data = .x))\n    )\n\n\ncoef_df &lt;- coef_df &lt;- true_coefs %&gt;%\n  get_coef_df(results,.)\n\nplot_coefficient_distribution(\n  coef_df,\n  title = \"Distribution of logistic regression parameter estimates \\nfit on data simulated from the same model\"\n  )\n\n\n\n\n\n\n\n\n\n\n\nSummary table for parameters of \\(n = 1000\\) logistic GLMs fit on simulated data with confounders\n\n\nVariable\nParameter estimate\nTrue value\nBias\n\n\n\n\n(Intercept)\n-0.0118677\n0.0\n-0.0118677\n\n\nW\n3.1185125\n3.0\n0.1185125\n\n\nX\n1.0791976\n1.0\n0.0791976\n\n\nZ\n1.5507121\n1.5\n0.0507121\n\n\n\n\n\nBasically the same results - no surprises there (we adjusted for the confounder)."
  },
  {
    "objectID": "projects/collapsibility/Simulating Collapsibility.html#scenario-3---confounders-present-prognostic-variable-omitted-1",
    "href": "projects/collapsibility/Simulating Collapsibility.html#scenario-3---confounders-present-prognostic-variable-omitted-1",
    "title": "Simulating collapsibility",
    "section": "Scenario 3 - Confounders present, prognostic variable omitted",
    "text": "Scenario 3 - Confounders present, prognostic variable omitted\nNow I remove \\(W\\) from the model fitting.\n\nresults &lt;- simulate_multiple_datasets(\n  n = 500,\n  sigma = cov_matrix_confounding,\n  n_simulations = 1000,\n  simulate_function = simulate_logistic_reg_model,\n  y_formula = 3*W + X + 1.5*Z,\n  x_threshold = qnorm(0.5) # Chosen so X is a 50/50 split in both treatment groups\n) %&gt;%\n  mutate(\n    fitted_model = map(data,~ glm(\n      outcome ~ X + Z,\n      family = binomial(link = \"logit\"),\n      data = .x))\n    )\n\n\ncoef_df &lt;- coef_df &lt;- true_coefs %&gt;%\n  get_coef_df(results,.)\n\nplot_coefficient_distribution(\n  coef_df,\n  title = \"Distribution of logistic regression parameter estimates \\nfit on data simulated from the same model\"\n  )\n\n\n\n\n\n\n\n\n\n\n\nSummary table for parameters of \\(n = 1000\\) logistic GLMs fit on simulated data with confounders\n\n\nVariable\nParameter estimate\nTrue value\nBias\n\n\n\n\n(Intercept)\n0.0064395\n0.0\n0.0064395\n\n\nX\n0.2457032\n1.0\n-0.7542968\n\n\nZ\n0.4046214\n1.5\n-1.0953786\n\n\n\n\n\nThe estimated coefficients are now way off (and in the wrong direction)!"
  }
]